# Docker Compose para desarrollo local
# Este archivo NO es necesario para Dockploy, pero puede ser útil para testing local

version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # Agregar otras variables de entorno según sea necesario
    # Volumes para desarrollo (comentados para producción):
    # volumes:
    #   - ./server:/app/server
    #   - ./cuba-connect-ui:/app/cuba-connect-ui

